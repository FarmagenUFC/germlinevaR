% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gvr_hpo.R
\name{gvr_hpo_genes}
\alias{gvr_hpo_genes}
\title{Resolve HPO phenotype terms to associated genes}
\usage{
gvr_hpo_genes(
  hpo,
  hpo_path = NULL,
  hpo_url = "https://purl.obolibrary.org/obo/hp/hpoa/phenotype_to_genes.txt",
  cache_dir = NULL,
  refresh_cache = FALSE,
  max_age_days = 30,
  verbose = TRUE
)
}
\arguments{
\item{hpo}{Character vector of HPO identifiers, e.g. \code{"HP:0003002"} or
\code{c("HP:0003002", "HP:0001939")}. Lenient input accepted (see Description).}

\item{hpo_path}{Optional path to a local \code{phenotype_to_genes.txt} file.
If supplied, no download is attempted.}

\item{hpo_url}{URL used when \code{hpo_path = NULL}. Default is the canonical
HPO/OBO phenotype-to-gene association file.}

\item{cache_dir}{Directory used to cache the downloaded HPO file. \code{NULL}
uses \code{tools::R_user_dir("germlinevaR", "cache")}.}

\item{refresh_cache}{Logical. If \code{TRUE}, force re-download of the HPO file
ignoring \code{max_age_days}. Default \code{FALSE}.}

\item{max_age_days}{Numeric. Automatic refresh threshold in days for the
cached HPO file. Default 30. Set to \code{Inf} to disable age-based refresh.}

\item{verbose}{Logical. If \code{TRUE}, print progress messages (download,
cache age, per-code resolution counts, normalisation events). Default TRUE.}
}
\value{
A sorted, upper-cased, deduplicated character vector of HGNC gene
symbols associated with the supplied HPO term(s). If nothing resolves,
returns \code{character(0)} and emits a warning listing the unresolved terms.
}
\description{
Retrieves genes associated with one or more Human Phenotype Ontology (HPO)
terms using the HPO phenotype-to-gene association table. This is mainly used
by \code{\link[=read.gvr]{read.gvr()}} and \code{\link[=gvr_filter]{gvr_filter()}} through their \code{hpo} argument, but can
also be called directly to inspect the gene set before filtering.

The HPO table is downloaded once and cached under
\code{tools::R_user_dir("germlinevaR", "cache")}. Cached files older than
\code{max_age_days} days trigger an automatic re-download; setting
\code{refresh_cache = TRUE} forces refresh regardless of age. Use
\verb{hpo_path = <local file>} for offline / air-gapped operation.

Input HPO identifiers are lenient-parsed: \code{"HP:0003002"}, \code{"hp:0003002"},
\code{"3002"}, and \code{"0003002"} are all accepted and normalised to canonical
\code{"HP:0003002"} before lookup. When normalisation happens a \code{message()} is
emitted so typos remain visible.

No ontology-descendant expansion is performed in this release: only genes
associated with the exact HPO terms supplied are returned. To include
descendants, resolve them yourself first (e.g. via \code{ontologyIndex::get_descendants()}).
}
\examples{
# Runnable example: use the tiny HPO fixture shipped in inst/extdata so
# the example needs no network access.
hpo_fx <- system.file("extdata", "hpo_phenotype_to_genes_mini.tsv",
                      package = "germlinevaR")
genes <- gvr_hpo_genes("HP:0003002", hpo_path = hpo_fx)
head(genes)

# Lenient input forms all normalise to the canonical HP:0003002:
identical(genes,
          gvr_hpo_genes("3002", hpo_path = hpo_fx))

# Multiple terms are unioned:
gvr_hpo_genes(c("HP:0003002", "HP:0025022"), hpo_path = hpo_fx)

\donttest{
# Network use (downloads and caches the full HPO table on first call):
# genes <- gvr_hpo_genes("HP:0003002")

# Force refresh of the cached HPO file:
# genes <- gvr_hpo_genes("HP:0003002", refresh_cache = TRUE)

# Use with read.gvr() to restrict to HPO-implicated genes:
# gvr <- read.gvr("path/to/vcfs", hpo = "HP:0003002")

# Or on an already-loaded table:
# gvr_flt <- gvr_filter(gvr, hpo = "HP:0003002")
}

}
\seealso{
Other germlinevaR: 
\code{\link{gvr_filter}()},
\code{\link{gvr_list_panels}()},
\code{\link{gvr_panel_genes}()},
\code{\link{gvr_plot}()},
\code{\link{gvr_summary}()},
\code{\link{read.gvr}()},
\code{\link{read.gvr.snpeff}()}
}
\concept{germlinevaR}
