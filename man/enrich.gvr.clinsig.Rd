% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich.gvr.clinsig.R
\name{enrich.gvr.clinsig}
\alias{enrich.gvr.clinsig}
\title{Enrich a germlinevaR MAF with clinical-significance predictions from MyVariant.info / dbNSFP}
\usage{
enrich.gvr.clinsig(
  maf,
  scope = c("missense_splice", "all", "has_clinsig", "panel"),
  panel = NULL,
  dbnsfp_columns = c("AlphaMissense_pred", "REVEL_score"),
  cache_path = NULL,
  batch_size = 500L,
  max_retries = 3L,
  request_timeout = 60L,
  offline_only = FALSE,
  on_collision = c("overwrite", "error"),
  verbose = FALSE
)
}
\arguments{
\item{maf}{A \code{data.table} / \code{data.frame} produced by \code{\link[=read.gvr]{read.gvr()}} or
\code{\link[=read.gvr.snpeff]{read.gvr.snpeff()}}. Must contain columns \code{NCBI_Build}, \code{Chromosome},
\code{Start_Position}, \code{Reference_Allele}, \code{Tumor_Seq_Allele2},
\code{Variant_Classification}, and (for default column placement) \code{CLIN_SIG}.}

\item{scope}{One of \code{"missense_splice"} (default), \code{"all"}, \code{"has_clinsig"},
or \code{"panel"}. Controls which rows are looked up against the API; rows
outside scope receive \code{NA} for the new columns.}

\item{panel}{Character vector of gene symbols. Only used when
\code{scope = "panel"}.}

\item{dbnsfp_columns}{Character vector of dbNSFP field tokens. Default:
\code{c("AlphaMissense_pred", "REVEL_score")}. See Details for the full token
list and the output-column naming convention.}

\item{cache_path}{Path to the SQLite cache file. If \code{NULL} (default),
\code{tools::R_user_dir("germlinevaR", which = "cache") / "clinsig.sqlite"} is
used. The parent directory is created if needed.}

\item{batch_size}{Variants per MyVariant.info POST (max 1000 per their docs;
default 500 = safety margin).}

\item{max_retries}{Maximum HTTP retries per batch on transient failures.
Default 3. Backoff: 1 s, 2 s, 4 s, 8 s (capped).}

\item{request_timeout}{HTTP timeout per request, in seconds. Default 60.}

\item{offline_only}{If \code{TRUE}, only consult the cache; never make network
calls. Uncached rows receive \code{NA}. Default \code{FALSE}.}

\item{on_collision}{Behavior when the input MAF already contains columns
that would be created by the enrichment step. \code{"overwrite"} (default)
drops the existing columns with a warning, \code{"error"} fails loud.}

\item{verbose}{If \code{TRUE}, print progress to the console. Default \code{FALSE}.}
}
\value{
A \code{data.table} with the same number of rows and the same row order
as \code{maf}, with new columns inserted immediately after \code{CLIN_SIG}. The
original \code{CLIN_SIG} column is preserved unchanged; downstream columns shift
right.
}
\description{
Augments the MAF returned by \code{\link[=read.gvr]{read.gvr()}} / \code{\link[=read.gvr.snpeff]{read.gvr.snpeff()}} with one or
more clinical-significance prediction columns (default: AlphaMissense
categorical verdict and REVEL ensemble score), placed immediately after the
existing \code{CLIN_SIG} column. Predictions are looked up from the free public
MyVariant.info REST API (no key, no registration) backed by dbNSFP. An
on-disk SQLite cache makes re-runs offline and fast.

\code{read.gvr()} and \code{read.gvr.snpeff()} are NOT modified by this enrichment
step. Their Phase N+2 byte-identity guarantee remains intact.
}
\details{
\subsection{Supported \code{dbnsfp_columns} tokens}{
\tabular{lll}{
\strong{Token}                \tab \strong{Output column}                  \tab \strong{Type} \cr
\code{"AlphaMissense_pred"}        \tab \code{CLIN_SIG_AlphaMissense}               \tab character (likely_pathogenic / ambiguous / likely_benign) \cr
\code{"AlphaMissense_score"}       \tab \code{CLIN_SIG_AlphaMissense_score}         \tab numeric 0-1 \cr
\code{"REVEL_score"}               \tab \code{CLIN_SIG_REVEL}                       \tab numeric 0-1 \cr
\code{"REVEL_rankscore"}           \tab \code{CLIN_SIG_REVEL_rankscore}             \tab numeric 0-1 \cr
\code{"CADD_phred"}                \tab \code{CLIN_SIG_CADD_phred}                  \tab numeric \cr
\code{"ClinPred_pred"}             \tab \code{CLIN_SIG_ClinPred}                    \tab character (D/T) \cr
\code{"MetaRNN_pred"}              \tab \code{CLIN_SIG_MetaRNN}                     \tab character (D/T) \cr
\code{"BayesDel_addAF_pred"}       \tab \code{CLIN_SIG_BayesDel}                    \tab character (D/T) \cr
\code{"PrimateAI_pred"}            \tab \code{CLIN_SIG_PrimateAI}                   \tab character (D/T)
}
}

\subsection{Build dispatch}{
\code{unique(maf$NCBI_Build)} is mapped to MyVariant.info's \code{assembly}
parameter:
\itemize{
\item \code{"GRCh37"} or \code{"hg19"} -> \code{assembly=hg19}
\item \code{"GRCh38"} or \code{"hg38"} -> \code{assembly=hg38}
\item Other labels (e.g. \code{"T2T-CHM13v2.0"}) -> error before any network call
}
}

\subsection{Scope semantics}{
\itemize{
\item \code{"missense_splice"} (default): \code{Variant_Classification} in
\code{c("Missense_Mutation","Splice_Site","Splice_Region")}. Matches the
coverage profile of dbNSFP (missense + splice SNVs).
\item \code{"all"}: every row queried; non-missense rows are mostly \code{NA}
because dbNSFP does not cover them.
\item \code{"has_clinsig"}: missense_splice intersected with rows whose
existing \code{CLIN_SIG} field is non-empty.
\item \code{"panel"}: missense_splice intersected with
\code{Hugo_Symbol \%in\% panel}.
}
}
}
\seealso{
\code{\link[=read.gvr]{read.gvr()}}, \code{\link[=read.gvr.snpeff]{read.gvr.snpeff()}}
}
