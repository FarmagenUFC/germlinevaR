% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gvr_domain_cache.R
\name{gvr_domain_cache_clear}
\alias{gvr_domain_cache_clear}
\title{Clear the auto-fetched protein-domain cache used by \code{gvr_lollipop}}
\usage{
gvr_domain_cache_clear(
  gene = NULL,
  organism = NULL,
  cache_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{gene}{Character(1) or \code{NULL}. Gene symbol to delete from the cache.
\code{NULL} (default) deletes every cached domain file in the cache directory
(i.e., every file matching \verb{^domains_interpro_.*\\\\.rds$}).}

\item{organism}{Integer or character or \code{NULL}. NCBI taxonomy id used as
part of the cache key. When \code{gene} is given but \code{organism} is \code{NULL},
every taxonomy variant of that gene is deleted (i.e., the pattern is
\verb{^domains_interpro_<GENE>_.*\\\\.rds$}). Default \code{NULL}.}

\item{cache_dir}{Character(1) or \code{NULL}. Override the cache directory.
\code{NULL} (default) triggers the precedence chain above.}

\item{verbose}{Logical(1). Print one line per deleted file. Default
\code{TRUE}.}
}
\value{
Invisibly, a character vector of the file paths that were
deleted (length 0 if none).
}
\description{
Deletes cached InterPro domain \code{.rds} files written by
\code{gvr_lollipop(..., domains = "auto")}. Useful when InterPro releases a new
version (roughly quarterly) and you want fresh annotations on the next
call. Also clears the matching in-memory session cache so the next call
actually re-fetches instead of returning the in-memory copy.
}
\details{
The cache directory is resolved with the same precedence chain used by
\code{gvr_lollipop()}, but here the chain only \strong{finds} an existing directory;
it never creates one. The precedence (first hit wins) is:
\enumerate{
\item \code{cache_dir} argument (explicit override).
\item Environment variable \code{GVR_CACHE_DIR}.
\item R option \code{getOption("germlinevaR.cache_dir")}.
\item \code{tools::R_user_dir("germlinevaR", "cache")}.
\item \code{file.path(tempdir(), "germlinevaR_cache")}.
}
If the resolved directory does not exist, the helper returns invisibly
with a \code{verbose} message and no files deleted (this is not an error;
a missing cache directory simply means there was nothing to clear).
}
\examples{
## Clear the cache for a specific gene (safe to run; no-op if cache is empty)
gvr_domain_cache_clear(gene = "TP53")

\donttest{
  ## Clear everything (all genes, all organisms)
  gvr_domain_cache_clear()

  ## Clear only TP53 across all organisms
  gvr_domain_cache_clear(gene = "TP53")

  ## Clear only human TP53
  gvr_domain_cache_clear(gene = "TP53", organism = 9606)
}
}
\seealso{
\code{\link[=gvr_lollipop]{gvr_lollipop()}}
}
