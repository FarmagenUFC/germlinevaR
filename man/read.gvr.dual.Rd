% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.gvr.dual.R
\name{read.gvr.dual}
\alias{read.gvr.dual}
\title{Convert dual-annotated germline VCF(s) (VEP + SnpEff) to an MAF-like data.table}
\usage{
read.gvr.dual(
  folder = ".",
  vcf_path = NULL,
  file = NULL,
  pattern = "\\\\.vcf\\\\.gz$",
  write_tsv = FALSE,
  write_rds = FALSE,
  write_xlsx = FALSE,
  out_dir = NULL,
  out_prefix = NULL,
  chunk_size = 25000L,
  ncbi_build = "auto",
  add_genotype = TRUE,
  strip_hgvs_prefix = TRUE,
  dedup_columns = TRUE,
  drop_empty_cols = FALSE,
  add_abraom = TRUE,
  abraom_path = NULL,
  abraom_url =
    "https://abraom.ib.usp.br/download/ABRaOM_60+_SABE_609_exomes_annotated.gz",
  cache_dir = NULL,
  min_DP = 10,
  min_GQ = 30,
  genes = NULL,
  panel = NULL,
  hpo = NULL,
  vc_nonSyn = FALSE,
  canonical_only = TRUE,
  ncores = 1L,
  normalize_alleles = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{folder}{Directory to scan in folder mode; every file matching \code{pattern} is
converted and row-bound. Default \code{"."}. Ignored when \code{vcf_path} is supplied.
Also used as the search root for \verb{file=}.}

\item{vcf_path}{Character vector of one or more full paths to \code{.vcf.gz}
files to convert. Use this to process a specific set of files outside
the folder pattern. Mutually exclusive with \verb{file=}. \code{NULL} (default)
selects folder mode.}

\item{file}{Character vector of basenames (e.g.
\code{c("S1.vep.vcf.gz", "S2.vep.vcf.gz")}) resolved against \verb{folder=}.
Use this to pick specific files from a folder that contains files you
do NOT want to merge. Mutually exclusive with \verb{vcf_path=}. \code{NULL}
(default) selects either \verb{vcf_path=} mode or folder-pattern mode.}

\item{pattern}{Regular expression identifying per-sample VCFs in folder mode.
Default \code{"\\\\.vcf\\\\.gz$"} (matches any \code{.vcf.gz} file). The old default
\code{"_\\\\d+(\\\\.(vep|snp[eE]ff))?\\\\.vcf\\\\.gz$"} (requires \verb{_NN} suffix) is
still available by passing it explicitly.}

\item{write_tsv}{Logical; if \code{TRUE}, also write the table as a TSV to \code{out_dir}.
Default \code{FALSE}.}

\item{write_rds}{Logical; if \code{TRUE}, also write the table as an \code{.rds} to \code{out_dir}.
Default \code{FALSE}.}

\item{write_xlsx}{Logical; if \code{TRUE}, also write the table as an \code{.xlsx} workbook
(single \code{"gvr_table"} sheet) to \code{out_dir}. Requires the \pkg{openxlsx} package (a
\code{Suggests} dependency); if it is not installed the export is skipped with a
warning. Default \code{FALSE}. Note: germline MAFs can be very large; Excel handles
them but the file is big and slow to open, so \code{write_rds}/\code{write_tsv} are better
for large tables. Excel also caps a sheet at 1,048,576 rows.}

\item{out_dir}{Output directory for written TSV/RDS/XLSX. \code{NULL} (default) uses the
input location/working directory. Only used when \code{write_tsv}/\code{write_rds}/\code{write_xlsx} is \code{TRUE}.}

\item{out_prefix}{Filename prefix for written outputs. \code{NULL} (default) derives one
from the input.}

\item{chunk_size}{Integer; number of VCF records processed per chunk (controls peak
memory and progress granularity). Default \code{25000L}.}

\item{ncbi_build}{Reference build label written into the table \code{NCBI_Build}
column. Default \code{"auto"}: the function inspects the input VCF header
(VEP \verb{assembly=} / SnpEff \code{SnpEffCmd} database token / first \verb{##contig=}
length) and picks the canonical label among \code{"GRCh38"}, \code{"GRCh37"},
\code{"T2T-CHM13v2.0"}. When detection cannot decide, falls back to \code{"GRCh38"}
with a verbose-mode message. Pass any literal (\code{"GRCh37"}, \code{"hg19"},
\code{"T2T-CHM13v2.0"}, internal lab codes) to override; the user-supplied
value is written verbatim into \code{NCBI_Build} and the rest of the pipeline
does not branch on its value. The ABraOM join uses dbSNP rsID + alleles
and is build-stable. The aliases \code{"hg19"} and \code{"hg38"} are mapped to
\code{"GRCh37"} / \code{"GRCh38"} for the mismatch check; a diagnostic warning
fires when an explicit \code{ncbi_build} value (canonical or alias) disagrees
with what auto-detection found at high confidence. Off-table user labels
(e.g. internal lab codes) pass through silently.}

\item{add_genotype}{Logical; if \code{TRUE} (default) add the \code{Genotype} column.}

\item{strip_hgvs_prefix}{Logical; if \code{TRUE} (default) strip the Ensembl feature
prefix from \code{HGVSc}/\code{HGVSp}.}

\item{dedup_columns}{Logical; if \code{TRUE} (default) drop duplicate-named columns when
byte-for-byte identical (otherwise keep and warn).}

\item{drop_empty_cols}{Logical; if \code{TRUE}, drop columns that are entirely \code{NA}/blank.
Default \code{FALSE}.}

\item{add_abraom}{Logical; if \code{TRUE} (default) join the ABraOM SABE-609 allele
frequency as \code{ABraOM_AF}.}

\item{abraom_path}{Path to a local ABraOM annotation file. \code{NULL} (default) uses an
auto-managed cache (see \code{cache_dir}), downloading from \code{abraom_url} if needed.}

\item{abraom_url}{URL of the ABraOM SABE-609 annotated release used when
\code{abraom_path} is \code{NULL}.}

\item{cache_dir}{Directory for the ABraOM reference cache (used only when
\code{abraom_path} is \code{NULL}). \code{NULL} (default) uses
\code{tools::R_user_dir("germlinevaR", "cache")}, which resolves to a
platform-appropriate directory (e.g. \verb{~/.cache/R/germlinevaR} on Linux).
The directory is created on first download. Set explicitly if you prefer a
custom location.}

\item{min_DP}{Numeric; keep only records with \code{DP > min_DP}. \code{NULL} (or \code{NA})
disables the depth filter. Default \code{10}.}

\item{min_GQ}{Numeric; keep only records with \code{GQ > min_GQ}. \code{NULL} (or \code{NA})
disables the genotype-quality filter. Default \code{30}.}

\item{genes}{Character vector of \code{Hugo_Symbol}s to keep (exact, case-insensitive),
or \code{NULL} (default) to keep all genes.}

\item{panel}{Character vector of curated disease panel name(s) (e.g.
\code{"breast cancer"}, \code{"hereditary prostate cancer"}, \code{"gist"}).
Each name is resolved to a gene vector via \code{\link[=gvr_panel_genes]{gvr_panel_genes()}} and the
union of all resolved genes is taken with \code{genes} (deduplicated,
uppercased). Names are matched case-insensitively, trimmed, and \verb{_} is
treated as a space, so \code{"Breast_Cancer"}, \code{"breast cancer"}, and
\code{" BREAST CANCER "} all resolve identically. A small alias table is
also recognised (e.g. \code{"gastrointestinal stromal tumor"} -> \code{"gist"},
the typo \code{"pheocromocytoma"} -> \code{"pheochromocytoma"}). The registry
currently ships 15 panels; see \code{\link[=gvr_list_panels]{gvr_list_panels()}} for the full list.
An unknown name raises an error listing the available panels. \code{NULL}
(default) disables panel filtering; behaviour is then byte-identical to
omitting the argument.}

\item{hpo}{Character vector of Human Phenotype Ontology (HPO) term
identifier(s), e.g. \code{"HP:0003002"} (Breast carcinoma) or
\code{c("HP:0003002", "HP:0025022")}. Each term is resolved to a gene
vector via \code{\link[=gvr_hpo_genes]{gvr_hpo_genes()}} and the union of all resolved genes is
added to any genes already produced by \code{genes} and \code{panel}
(deduplicated, uppercased). Lenient input is accepted: \code{"HP:0003002"},
\code{"hp:0003002"}, \code{"3002"}, and \code{"0003002"} all normalise to the
canonical \code{"HP:0003002"} form. Only exact-term associations are used
(no ontology-descendant expansion). The HPO phenotype-to-gene table is
downloaded and cached under \code{tools::R_user_dir("germlinevaR", "cache")}
and auto-refreshed after 30 days; see \code{\link[=gvr_hpo_genes]{gvr_hpo_genes()}} for offline /
air-gapped usage via \verb{hpo_path=}. \code{NULL} (default) disables HPO
filtering; behaviour is then byte-identical to omitting the argument.}

\item{vc_nonSyn}{Logical or character vector. Controls which
\code{Variant_Classification} values are retained (mirroring the
convention of the \code{vc_nonSyn} argument). \code{FALSE} (default) keeps ALL variant classifications.
\code{TRUE} keeps only protein-altering classes (High/Moderate VEP consequences):
\code{"Frame_Shift_Del"}, \code{"Frame_Shift_Ins"}, \code{"Splice_Site"},
\code{"Translation_Start_Site"}, \code{"Nonsense_Mutation"}, \code{"Nonstop_Mutation"},
\code{"In_Frame_Del"}, \code{"In_Frame_Ins"}, \code{"Missense_Mutation"}. Alternatively,
pass a custom character vector of classifications to keep. Rows with
missing/blank \code{Variant_Classification} are always removed when this filter
is active.}

\item{canonical_only}{Logical; when \code{TRUE} (default), drops table rows whose
chosen VEP CSQ block has \code{CANONICAL != "YES"}. read.gvr() already prefers
\code{CANONICAL=YES} when ranking CSQ blocks; this filter discards the
fallback rows emitted when no canonical block exists for a given ALT.
For SnpEff-annotated input, the ANN field has no CANONICAL flag —
\code{canonical_only=TRUE} is ignored with a warning, and the result is the
same as \code{canonical_only=FALSE}.}

\item{ncores}{Integer; number of worker processes for converting MULTIPLE input
files in parallel via \code{\link[parallel:mcdummies]{parallel::mclapply()}} (fork-based; Unix/macOS only).
Default \code{1L} runs sequentially and is byte-identical to previous behaviour.
Values \verb{> 1} only help when more than one VCF is being read (each file is an
independent task) and are clamped to \code{min(ncores, detectCores(), n_files)}. On
non-fork platforms it falls back to sequential. A single file is unaffected.}

\item{normalize_alleles}{Logical; if \code{TRUE} (default, since 0.99.2) apply
bcftools-norm-style trimming of common REF/ALT prefix and suffix nucleotides
before deriving MAF-like coords (\code{Start_Position}, \code{Reference_Allele},
\code{Tumor_Seq_Allele2}). This is the recommended behaviour: it puts each
variant on its unique minimal representation and prevents distinct multi-ALT
records from collapsing to the same MAF key (which could previously drop or
scramble annotations on the SnpEff side of the dual reader; VEP-only reads
were unaffected). Set \code{FALSE} to reproduce pre-0.99.2 coords for
reproducibility with an older analysis; note this is not recommended for
new research.}

\item{verbose}{Logical; if \code{TRUE} (default) print per-file and per-chunk progress
(file i/N, cumulative records, elapsed seconds).}
}
\value{
A \code{data.table} with one row per ALT allele, ~124 columns. Carries
\code{attr(., "annotator") = "dual"}.
}
\description{
Converts single-sample germline VCFs that carry \strong{both} Ensembl VEP \code{CSQ}
and SnpEff \code{ANN} annotation INFO fields on the same records (typical
workflow: SnpEff -> VEP, or vice versa). Returns an MAF-like
\code{data.table} with one row per ALT allele, using VEP's transcript pick as
the spine and adding SnpEff-derived comparison columns and LoF/NMD
predictions.

This function is the dual-annotator sibling of \code{\link[=read.gvr]{read.gvr()}} (VEP-only) and
\code{\link[=read.gvr.snpeff]{read.gvr.snpeff()}} (SnpEff-only). All three are normally invoked through
\code{\link[=read.gvr]{read.gvr()}}, which auto-routes inputs based on the INFO tags found in the
VCF header.
}
\details{
Field-level priority: \strong{VEP wins.} The canonical table columns and all 80
(or 81 with \code{FREQS}) VEP-style CSQ columns hold VEP's pick exclusively.
SnpEff data is added in additional columns:
\itemize{
\item \code{snpeff_consequence}, \code{snpeff_impact}, \code{snpeff_gene}, \code{snpeff_hgvsc}
-- the SnpEff ANN block matching the same ALT allele and gene as VEP's
pick (most-deleterious block when multiple match). Empty when SnpEff
has no ANN block for that allele.
\item \code{LOF_Gene}, \code{LOF_Pct_Transcripts} -- SnpEff's loss-of-function
prediction (gene flagged, fraction of transcripts affected) from the
INFO \verb{LOF=} field. Empty when no LoF call.
\item \code{NMD_Gene}, \code{NMD_Pct_Transcripts} -- SnpEff's nonsense-mediated-
decay prediction from the INFO \verb{NMD=} field. Empty when no NMD call.
}

Transcript pick: VEP drives the row spine (one row per ALT allele). If VEP
was run with \code{--per_gene} (one transcript per gene per allele), the chosen
transcript is VEP's most-severe across genes for that allele; otherwise the
standard VEP most-severe-block ranking applies. SnpEff annotations are
attached \emph{to} that row from the ANN block matching the same allele and
(preferentially) the same \code{Gene_Name}.

Schema additions vs \code{read.gvr()} (VEP-only):
\itemize{
\item \code{FREQS} -- 81st canonical CSQ field, populated when VEP was run
with \code{--everything} (v113+). Names the gnomAD population where MAX_AF
was observed (e.g. "gnomADg_ASJ"). Empty when the CSQ header does not
include this field.
\item 4 LOF/NMD columns (above)
\item 4 snpeff_* parallel columns (above)
}
Total: ~124 columns (vs ~116 for \code{read.gvr()} and \code{read.gvr.snpeff()}).

All other behaviour, defaults, arguments, and filtering options are
identical to \code{\link[=read.gvr]{read.gvr()}}; see that function's documentation for the full
option reference.
}
\examples{
## The function signature is exported and callable:
is.function(read.gvr.dual)

\donttest{
## read.gvr.dual() expects VCFs annotated with BOTH VEP (CSQ INFO field)
## AND SnpEff (ANN INFO field) in the same record. The shipped example
## VCF is VEP-only, so a real dual-annotated example needs your own
## VCFs; we therefore guard the call on the directory existing, so the
## example skips cleanly on machines without the data.
dual_dir <- "/path/to/dual-annotated-vcfs/"
if (dir.exists(dual_dir)) {
    gvr <- read.gvr.dual(folder = dual_dir)

    ## Or via the auto-router in read.gvr() when the VCF header declares
    ## both VEP and SnpEff INFO fields:
    gvr <- read.gvr(dual_dir)

    ## Compare VEP vs SnpEff picks on high-impact variants:
    gvr[IMPACT == "HIGH" & snpeff_impact != "" & IMPACT != snpeff_impact,
        .(Hugo_Symbol, Consequence, IMPACT, snpeff_gene, snpeff_consequence,
            snpeff_impact)]
}
}
}
\seealso{
\code{\link[=read.gvr]{read.gvr()}}, \code{\link[=read.gvr.snpeff]{read.gvr.snpeff()}}
}
