% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gvr_plot.R
\name{gvr_plot}
\alias{gvr_plot}
\title{Cohort top-genes variant matrix from a germline MAF (read.gvr / gvr_filter output)}
\usage{
gvr_plot(
  maf,
  top_n = 20,
  sample_col = "Tumor_Sample_Barcode",
  out_dir = ".",
  file_prefix = "gvr_plot",
  sample_name_rot = 45,
  gene_name_size = 10,
  sample_name_size = 10,
  axis_tick_size = 7,
  legend_label_size = 10,
  legend_title_size = 10,
  legend_label_wrap_chars = Inf,
  impact_title_side = c("left", "right"),
  verbose = TRUE
)
}
\arguments{
\item{maf}{A \code{data.table}/\code{data.frame} MAF from \code{\link[=read.gvr]{read.gvr()}} or \code{\link[=gvr_filter]{gvr_filter()}}.
Required columns: \code{Hugo_Symbol}, \code{Variant_Classification}.}

\item{top_n}{Integer; number of genes (rows) shown, ranked by number of distinct
samples mutated then by variant count. Default \code{20}.}

\item{sample_col}{Name of the per-sample column. Default \code{"Tumor_Sample_Barcode"}.
If absent, all rows are pooled into a single sample \code{"All"} (with a warning).}

\item{out_dir}{Output directory for the PNG. Created if it does not exist.
Default \code{"."} (current working directory).}

\item{file_prefix}{Base filename for the written PNG. Default \code{"gvr_plot"};
the file is written as \verb{<file_prefix>.png} (fixed name, no timestamp), e.g.
\code{gvr_plot.png}. An existing file at that path is overwritten (a message is
emitted when \code{verbose = TRUE}).}

\item{sample_name_rot}{Numeric; rotation angle (degrees) for the sample-name
labels at the top of the heatmap. Default \code{45}. Common alternatives are
\code{0} (horizontal) and \code{90} (vertical). Must be a single finite numeric.}

\item{gene_name_size}{Numeric; font size in points for the gene-name row
labels (left side of the heatmap). Default \code{10}. Must be a single
finite positive numeric.}

\item{sample_name_size}{Numeric; font size in points for the sample-name
column labels (top of the heatmap). Default \code{10}. Must be a single
finite positive numeric.}

\item{axis_tick_size}{Numeric; font size in points for the y-axis tick
labels of the top "Variant impact" bar and the right gene-burden bar.
Default \code{7}. Must be a single finite positive numeric.}

\item{legend_label_size}{Numeric; font size in points for the label text
of both side legends ("Impact" and "Most severe class"). Default \code{10}.
Must be a single finite positive numeric.}

\item{legend_title_size}{Numeric; font size in points for the title text
of both side legends ("Impact" and "Most severe class"). The bold
face is preserved. Default \code{10}. Must be a single finite positive
numeric.}

\item{legend_label_wrap_chars}{Numeric; if a (prettified) legend label is
longer than this many characters, it is wrapped onto two lines at the
space closest to the middle. Default \code{Inf} disables wrapping (so all
labels stay on one line, matching the legacy behaviour). Use a finite
integer (e.g. \code{14}) when long labels would clip the right edge at
large \code{legend_label_size}. Labels without internal spaces (e.g.
\verb{5'UTR}, \code{RNA}) are never wrapped regardless of length.}

\item{impact_title_side}{One of \code{"left"} (default) or \code{"right"}; controls
where the \code{"Variant impact"} annotation title is drawn relative to the
top stacked-bar panel. \code{"left"} renders the title vertically (acting as a
y-axis title for the impact panel); \code{"right"} renders it horizontally
on the right of the panel (the previous default).}

\item{verbose}{Logical; if \code{TRUE} (default) print the path of the file written.}
}
\value{
Invisibly, the path of the written PNG (character), or \code{NA_character_} if
the plot was skipped (\pkg{ComplexHeatmap} not installed, or no known-gene
variants present).
}
\description{
Draws a maftools-style top-genes variant matrix from a MAF-style table - the output of
\code{\link[=read.gvr]{read.gvr()}}, or of \code{\link[=gvr_filter]{gvr_filter()}} - and writes it to a PNG file. Rows are the
top-\code{top_n} genes (ranked by number of distinct samples mutated, then by variant
count); columns are samples. Each gene x sample cell shows the single MOST-SEVERE
\code{Variant_Classification} observed for that gene in that sample. The plot is drawn
with \pkg{ComplexHeatmap}.
}
\details{
This is the standalone top-genes variant matrix previously produced inside \code{\link[=gvr_summary]{gvr_summary()}}. It
needs only the \code{Hugo_Symbol} and \code{Variant_Classification} columns (plus the
per-sample column).

Cell collapse: when a gene has several variant classes in one sample, the cell is
coloured by the most severe class, using this ranking (high to low):
Translation_Start_Site, Nonsense_Mutation, Nonstop_Mutation, Splice_Site,
Frame_Shift_Del, Frame_Shift_Ins, In_Frame_Del, In_Frame_Ins, Missense_Mutation,
Splice_Region, Protein_altering_variant, Silent, 5'UTR, 3'UTR, 5'Flank, 3'Flank,
RNA, Intron, IGR, Targeted_Region. Any class outside this list ranks last and is
coloured grey. Colours follow a colourblind-safe (Okabe-Ito) palette.

Annotations: a right-side bar shows each gene's total variant burden; a top bar
shows each sample's total variant burden (axis labelled in thousands). Empty cells
(gene not mutated in that sample) are light grey.

Data conventions:
\itemize{
\item "Missing" means \code{NA} OR empty string \code{""}.
\item Unknown/blank gene symbols are \code{Hugo_Symbol} in \code{c(".", "", "Unknown")};
these are excluded from the top-genes variant matrix.
\item Works on ANY MAF-shaped table; it makes no assumption about prior
filtering. It is commonly run on \code{\link[=gvr_filter]{gvr_filter()}} output.
}
}
\section{Dependencies}{

Requires \pkg{ComplexHeatmap} (a \pkg{Bioconductor} package, listed in \code{Suggests}).
If it is not installed the top-genes variant matrix is skipped with a warning and \code{NA_character_}
is returned.
}

\examples{
\dontrun{
maf <- read.gvr("/path/to/vcf_folder")

## write a top-20 variant matrix to the current directory
p <- gvr_plot(maf)
p                                  # path to the PNG

## smaller top-genes variant matrix of filtered hits, into a results folder
gvr_plot(gvr_filter(maf), top_n = 15, out_dir = "results/plots")
}

}
\seealso{
\code{\link[=gvr_summary]{gvr_summary()}} for the tabular summary, \code{\link[=read.gvr]{read.gvr()}} to build the MAF,
\code{\link[=gvr_filter]{gvr_filter()}} to filter it before plotting.

Other germlinevaR: 
\code{\link{gvr_filter}()},
\code{\link{gvr_list_panels}()},
\code{\link{gvr_panel_genes}()},
\code{\link{gvr_summary}()},
\code{\link{read.gvr}()},
\code{\link{read.gvr.snpeff}()}
}
\author{
germlinevaR authors
}
\concept{germlinevaR}
