% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gvr_novel.R
\name{gvr_novel}
\alias{gvr_novel}
\title{Filter a read.gvr MAF down to candidate novel variants}
\usage{
gvr_novel(maf, verbose = TRUE)
}
\arguments{
\item{maf}{data.table / data.frame from \code{\link[=read.gvr]{read.gvr()}} (or compatible, e.g. the
output of \code{\link[=gvr_filter]{gvr_filter()}}). Filtered in a copy; the input object is not
modified.}

\item{verbose}{logical(1). If \code{TRUE} (default), print a step-by-step audit
line for each of the 4 cascaded filters showing rows-in -> rows-out and
percentage retained, ending with a one-line summary.}
}
\value{
A \code{data.table} with the same columns as \code{maf} but only the rows that
pass all four "no database evidence" checks. If none of the 4 columns is
present in \code{maf}, the output equals the input and a warning is issued.
}
\description{
Returns only the rows of a \code{read.gvr()} / \code{gvr_filter()} MAF that have NO
database evidence in any of the standard catalogues: no rsID in \code{dbSNP_RS},
AND no allele frequency in \code{gnomADe_AF}, \code{AF}, or \code{ABraOM_AF}. This is the
canonical "show me only the novel variants" subsetter and complements
\code{\link[=gvr_filter]{gvr_filter()}} (which uses presence of AF as a continuous filter).
}
\details{
A column value counts as "missing/empty" iff \code{is.na(x) | x == ""}. This is
the same convention used throughout the package (see \code{\link[=gvr_filter]{gvr_filter()}} and
\code{\link[=gvr_summary]{gvr_summary()}}): \code{read.gvr()} writes \code{""} for absent values, but NA can
creep in if upstream code re-coerced columns.

A row is kept iff ALL FOUR of the following columns are missing/empty:
\itemize{
\item \code{dbSNP_RS}     - rsID from VEP \code{Existing_variation}
\item \code{gnomADe_AF}   - gnomAD exome allele frequency
\item \code{AF}           - gnomAD genome / VCF allele frequency
\item \code{ABraOM_AF}    - ABraOM SABE-609 Brazilian-cohort allele frequency
}
If any of the four columns is absent from the input (e.g. ABraOM disabled
at read time), the function treats that column as "all missing" - i.e. it
does not exclude any row on the basis of the missing column. The audit log
will say \verb{(column not present, skipped)} for transparency.

The cascade order is fixed (dbSNP_RS -> gnomADe_AF -> AF -> ABraOM_AF) so
the verbose audit is reproducible across runs.

The input is never modified: filtering operates on an internal \code{data.table}
copy and the output has the same column set and order as the input.
}
\examples{
\dontrun{
  maf <- read.gvr("vcf_dir/", pattern = "\\\\.vep\\\\.vcf\\\\.gz$")
  f   <- gvr_filter(maf)
  nov <- gvr_novel(f)
  ## quick sanity: every kept row really is novel
  stopifnot(all(is.na(nov$dbSNP_RS)   | nov$dbSNP_RS   == ""))
  stopifnot(all(is.na(nov$gnomADe_AF) | nov$gnomADe_AF == ""))
}

}
\seealso{
\code{\link[=read.gvr]{read.gvr()}}, \code{\link[=gvr_filter]{gvr_filter()}}, \code{\link[=gvr_summary]{gvr_summary()}}
}
