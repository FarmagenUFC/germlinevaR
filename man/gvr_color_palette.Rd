% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gvr_palette.R
\name{gvr_color_palette}
\alias{gvr_color_palette}
\title{Generate a Vector of Colors From a Named Palette}
\usage{
gvr_color_palette(palette = "okabe_ito", n = 8L)
}
\arguments{
\item{palette}{Character string. Name of the palette to use.  See
\code{\link[=gvr_list_palettes]{gvr_list_palettes()}} for the full catalog of 52 names.  Defaults to
\code{"okabe_ito"}, the colorblind-friendly palette recommended by
Okabe & Ito (2008).}

\item{n}{Integer.  Number of colors to return.  When \code{n} exceeds the
base length of a discrete palette, intermediate colors are interpolated
via \code{\link[grDevices:colorRamp]{grDevices::colorRampPalette()}}.}
}
\value{
A character vector of length \code{n} containing hex color codes.
}
\description{
\code{gvr_color_palette()} returns a character vector of \code{n} hex codes drawn
from one of 52 named palettes covering ColorBrewer, viridis, scientific
journals, publishers, corporate brands, colorblind-friendly schemes,
gradients, and classic R palettes.  This is the helper that
\code{\link[=gvr_lollipop]{gvr_lollipop()}} uses internally to resolve the \code{variant_palette} and
\code{domain_palette} arguments, but it is also exported so users can build
their own color vectors for any plot.
}
\details{
The 52 palettes are grouped into the following families.  Names are
identical to those exposed by the sibling \code{plot_multiple_compounds()}
function so the two packages share the same color vocabulary.

\emph{Base:} \code{"hue"}, \code{"ggplot2"}, \code{"default"}  (the ggplot2 default).

\emph{ColorBrewer qualitative (good for distinct categories):}
\code{"set1"}, \code{"set2"}, \code{"set3"}, \code{"dark2"}, \code{"paired"}, \code{"accent"},
\code{"pastel1"}, \code{"pastel2"}.

\emph{ColorBrewer sequential (ordered data):}
\code{"blues"}, \code{"reds"}, \code{"greens"}, \code{"purples"}, \code{"oranges"}, \code{"greys"}.

\emph{ColorBrewer diverging:} \code{"spectral"}, \code{"rdylbu"}, \code{"rdylgn"},
\code{"piyg"}, \code{"prgn"}.

\emph{Viridis (perceptually uniform):} \code{"viridis"}, \code{"magma"},
\code{"inferno"}, \code{"plasma"}.

\emph{Journal palettes:} \code{"nature"}, \code{"science"}, \code{"cell"}, \code{"plos"},
\code{"elife"}.

\emph{Publisher palettes:} \code{"bmc"}, \code{"frontiers"}, \code{"wiley"}, \code{"elsevier"},
\code{"oxford"}, \code{"springer"}, \code{"acs"}, \code{"rsc"}.

\emph{Corporate brand palettes:} \code{"ibm"}, \code{"google"}, \code{"microsoft"},
\code{"twitter"}.

\emph{Colorblind-friendly:} \code{"okabe_ito"}, \code{"colorblind"}, \code{"cud"},
\code{"tol"}.

\emph{Gradient palettes:} \code{"blue_red"}, \code{"green_red"}, \code{"purple_orange"},
\code{"cool_warm"}, \code{"blue_yellow"}.

\emph{Classic R:} \code{"rainbow"}, \code{"heat"}, \code{"terrain"}, \code{"topo"}, \code{"cm"}.

\strong{Optional dependencies:}
ColorBrewer palettes use \pkg{RColorBrewer} when available and fall back
to interpolated base colors otherwise.  Viridis palettes use
\pkg{viridisLite} when available and fall back to manual hex sequences
otherwise.  Both packages are in \verb{Suggests:}.
}
\examples{
# Get 8 Nature-journal colors
gvr_color_palette("nature", 8)

# Default Okabe-Ito palette for 6 categories
gvr_color_palette(n = 6)

# List all available palette names
gvr_list_palettes()

\donttest{
  if (requireNamespace("ggplot2", quietly = TRUE)) {
    # Use directly in a ggplot2 plot
    ggplot2::ggplot(iris,
        ggplot2::aes(Sepal.Length, Sepal.Width, color = Species)) +
      ggplot2::geom_point() +
      ggplot2::scale_color_manual(
        values = gvr_color_palette("viridis", 3))
  }
}
}
\seealso{
\code{\link[=gvr_list_palettes]{gvr_list_palettes()}}, \code{\link[=gvr_lollipop]{gvr_lollipop()}}.
}
