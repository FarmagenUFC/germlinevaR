% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gvr_filter.R
\name{gvr_filter}
\alias{gvr_filter}
\title{Modular, individually-toggleable filtering of a read.gvr table}
\usage{
gvr_filter(
  gvr,
  gnomADe_AF = 0.01,
  AF = 0.01,
  ABraOM_AF = 0.01,
  gnomADe_AF_keep_missing = TRUE,
  AF_keep_missing = TRUE,
  ABraOM_AF_keep_missing = TRUE,
  clin_sig_terms = c("likely_pathogenic", "pathogenic", "uncertain_significance"),
  clin_sig_keep_missing = TRUE,
  remove_benign = FALSE,
  biotype_keep = NULL,
  gt_exclude = c("0", "0/0"),
  vc_nonSyn = FALSE,
  missense_only = FALSE,
  genes = NULL,
  save_excel = FALSE,
  out_dir = NULL,
  file_prefix = "gvr_filter",
  verbose = TRUE
)
}
\arguments{
\item{gvr}{data.table / data.frame from \code{\link[=read.gvr]{read.gvr()}} (or compatible). Filtered in a
copy; the input object is not modified.}

\item{gnomADe_AF}{Numeric upper threshold for the gnomAD exome AF column
\code{gnomADe_AF} (keep rows with AF < threshold). \code{NULL} disables this filter.
Default 0.01.}

\item{AF}{Numeric upper threshold for the \code{AF} column (gnomAD genome / VCF allele
frequency). \code{NULL} disables this filter. Default 0.01.}

\item{ABraOM_AF}{Numeric upper threshold for the Brazilian-cohort (ABraOM SABE 609)
column \code{ABraOM_AF}. \code{NULL} disables this filter. Default 0.01.}

\item{gnomADe_AF_keep_missing}{Logical; if TRUE (default), keep rows whose
\code{gnomADe_AF} is missing (NA or ""); if FALSE drop them. Ignored when
\code{gnomADe_AF} is NULL.}

\item{AF_keep_missing}{Logical; missing-value handling for the \code{AF} filter.
TRUE (default) keeps missing. Ignored when \code{AF} is NULL.}

\item{ABraOM_AF_keep_missing}{Logical; missing-value handling for the ABraOM
filter. TRUE (default) retains variants absent from the Brazilian cohort
(where absence often means "not catalogued", not "common"). Ignored when
\code{ABraOM_AF} is NULL.}

\item{clin_sig_terms}{Character vector of clinical-significance terms to keep
(substring, case-insensitive, OR-combined). \code{NULL} disables the CLIN_SIG filter.
Default: c("likely_pathogenic","pathogenic","uncertain_significance").}

\item{clin_sig_keep_missing}{Logical; if TRUE (default) rows with missing CLIN_SIG
(NA/"") are kept. Only relevant when \code{clin_sig_terms} is non-NULL.}

\item{remove_benign}{Logical; if TRUE, remove rows whose \code{CLIN_SIG} contains
"benign" (substring, case-insensitive). This catches \code{benign},
\code{likely_benign}, and compound annotations like
\code{"uncertain_significance&likely_benign"}. Applied AFTER the \code{clin_sig_terms}
keep-filter, so a row that matched a wanted term but also contains "benign"
is still removed. \code{FALSE} (default) does not remove benign rows.}

\item{biotype_keep}{Character vector of BIOTYPE values to keep (exact match via \%in\%).
\code{NULL} (default) disables the biotype filter — all biotypes are kept.
Pass e.g. \code{c("protein_coding", "protein_coding_LoF")} to restrict to
protein-coding transcripts.}

\item{gt_exclude}{Character vector of GT values to remove (exact match). \code{NULL}
disables the genotype filter. Default: c("0","0/0").}

\item{vc_nonSyn}{Logical or character vector. Controls which
\code{Variant_Classification} values are retained. \code{FALSE} (default) keeps all.
\code{TRUE} keeps only the 9 protein-altering classes (Frame_Shift_Del,
Frame_Shift_Ins, Splice_Site, Translation_Start_Site, Nonsense_Mutation,
Nonstop_Mutation, In_Frame_Del, In_Frame_Ins, Missense_Mutation).
A custom character vector keeps only those classifications. Rows with
missing/blank \code{Variant_Classification} are removed when this filter is active.}

\item{missense_only}{Logical; if \code{TRUE}, keep only rows whose
\code{Variant_Classification} equals \code{"Missense_Mutation"} (added in vN+5).
Default \code{FALSE} preserves prior behaviour byte-for-byte. Combines
non-contradictorily with \code{vc_nonSyn}: \code{vc_nonSyn} runs first (keeping
9 protein-altering classes), then \code{missense_only} narrows to the missense
subset. Errors with a clear message if \code{Variant_Classification} is missing.}

\item{genes}{Character vector of \code{Hugo_Symbol}s to keep (exact, case-insensitive),
or \code{NULL} (default) to keep all genes.}

\item{save_excel}{Logical; if TRUE, also write the FILTERED table to an \code{.xlsx}
workbook (single \code{"Filtered"} sheet) at \verb{<out_dir>/<file_prefix>.xlsx}. Requires
the \pkg{openxlsx} package (a \code{Suggests} dependency); if it is not installed the
export is skipped with a warning. Default FALSE. The write is a side effect only:
the returned \code{data.table} is identical whether or not \code{save_excel} is TRUE.}

\item{out_dir}{Output directory for the Excel file. \code{NULL} (default) uses the
current working directory. Created if it does not exist. Only used when
\code{save_excel = TRUE}.}

\item{file_prefix}{Filename prefix (without extension) for the Excel file. Default
\code{"gvr_filter"} -> \code{gvr_filter.xlsx}. Only used when \code{save_excel = TRUE}.}

\item{verbose}{Logical; if TRUE (default) print a per-filter breakdown (rows in -> out
and rows removed by each active step).}
}
\value{
A \code{data.table} of the surviving rows, with the same columns as the input.
A plain \code{data.frame} input is returned as a \code{data.table}. The input object is not
modified. With \code{verbose = TRUE}, a per-filter breakdown (rows in -> out, and rows
removed by each active step) is printed as it runs.
}
\description{
Applies a set of independent variant filters to the MAF-like table produced by
\code{\link[=read.gvr]{read.gvr()}}. Each distinct filter is its own argument; setting an argument to
\code{NULL} disables that filter entirely (no rows removed by it). With all defaults,
\code{gvr_filter(gvr)} reproduces the canonical rare / clinically-relevant /
called-genotype pipeline (AF filters + CLIN_SIG + GT exclusion).
}
\details{
Filters are applied in a fixed order; each step operates on the survivors of the
previous one:
\enumerate{
\item gnomAD exome AF        - \code{gnomADe_AF}        (+ \code{gnomADe_AF_keep_missing})
\item gnomAD genome / VCF AF - \code{AF}                (+ \code{AF_keep_missing})
\item ABraOM AF             - \code{ABraOM_AF}         (+ \code{ABraOM_AF_keep_missing})
\item Clinical significance  - \code{clin_sig_terms}    (+ \code{clin_sig_keep_missing})
\item Remove benign          - \code{remove_benign}
\item Biotype                - \code{biotype_keep}
\item Genotype exclusion     - \code{gt_exclude}
\item Variant classification - \code{vc_nonSyn}
\item Gene subset            - \code{genes}
}

Important data notes (true of \code{\link[=read.gvr]{read.gvr()}} output):
\itemize{
\item AF columns are CHARACTER (e.g. \code{"0.8781"}), so they are coerced with
\code{as.numeric()} before comparison.
\item "Missing" means EITHER \code{NA} OR empty string \code{""} (\code{\link[=read.gvr]{read.gvr()}} uses \code{""}
for absent values). Both are treated as missing everywhere in this function.
\item \code{CLIN_SIG} matching is SUBSTRING + case-insensitive. A compound annotation
such as \code{"pathogenic&benign"} or
\code{"uncertain_significance&likely_benign&benign"} is KEPT because it CONTAINS a
wanted term. This matches the dplyr \code{str_detect()} / \code{grepl()} convention. Use
exact-token matching only if you split \code{CLIN_SIG} yourself.
\item The default \code{gt_exclude = c("0", "0/0")} is a no-op on data whose \code{GT}
column only contains called alt genotypes (e.g. \code{0/1}, \code{1/1}, \code{1/2}); it is
retained for portability to data that does carry \code{"0"}/\code{"0/0"}.
}

The input is never modified: filtering operates on an internal \code{data.table} copy.
}
\examples{
## Load the shipped example table
gvr <- readRDS(system.file("extdata", "example_gvr.rds",
                           package = "germlinevaR"))
## Default filter (rare + clinically relevant + called genotypes).
## The example table was built without ABraOM annotation, so we
## disable the ABraOM_AF filter to avoid a "column not found" warning.
filt <- gvr_filter(gvr, ABraOM_AF = NULL, verbose = FALSE)
dim(filt)

## Variations on the default pipeline using the shipped example table

## Add protein-coding biotype filter:
gvr_filter(gvr, ABraOM_AF = NULL,
           biotype_keep = c("protein_coding", "protein_coding_LoF"),
           verbose = FALSE)

## Only the rarity filter on gnomAD exome AF, nothing else:
gvr_filter(gvr, gnomADe_AF = 0.001, AF = NULL, ABraOM_AF = NULL,
           clin_sig_terms = NULL, gt_exclude = NULL,
           vc_nonSyn = FALSE, genes = NULL, verbose = FALSE)

## Pathogenic-only, protein-coding:
gvr_filter(gvr, ABraOM_AF = NULL,
           clin_sig_terms = c("pathogenic", "likely_pathogenic"),
           biotype_keep = "protein_coding", verbose = FALSE)

## Remove benign annotations (including likely_benign and compound entries):
gvr_filter(gvr, ABraOM_AF = NULL, remove_benign = TRUE, verbose = FALSE)

## Keep only protein-altering variants and a gene panel:
gvr_filter(gvr, ABraOM_AF = NULL, vc_nonSyn = TRUE,
           genes = c("TP53", "BRCA1", "BRCA2"), verbose = FALSE)
}
\seealso{
\code{\link[=read.gvr]{read.gvr()}} to build the table, \code{\link[=gvr_summary]{gvr_summary()}} to summarise the filtered
variants.

Other germlinevaR: 
\code{\link{gvr_list_panels}()},
\code{\link{gvr_panel_genes}()},
\code{\link{gvr_plot}()},
\code{\link{gvr_summary}()},
\code{\link{read.gvr}()},
\code{\link{read.gvr.snpeff}()}
}
\author{
germlinevaR authors
}
\concept{germlinevaR}
