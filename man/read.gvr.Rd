% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.gvr.R
\name{read.gvr}
\alias{read.gvr}
\title{Convert VEP-annotated germline VCF(s) to a maftools-style MAF data.table}
\usage{
read.gvr(
  folder = ".",
  vcf_path = NULL,
  pattern = "_\\\\d+\\\\.vep\\\\.vcf\\\\.gz$",
  write_tsv = FALSE,
  write_rds = FALSE,
  write_xlsx = FALSE,
  out_dir = NULL,
  out_prefix = NULL,
  chunk_size = 25000L,
  ncbi_build = "GRCh38",
  add_genotype = TRUE,
  strip_hgvs_prefix = TRUE,
  dedup_columns = TRUE,
  drop_empty_cols = FALSE,
  add_abraom = TRUE,
  abraom_path = NULL,
  abraom_url =
    "https://abraom.ib.usp.br/download/ABRaOM_60+_SABE_609_exomes_annotated.gz",
  min_DP = 10,
  min_GQ = 30,
  genes = NULL,
  ncores = 1L,
  verbose = TRUE
)
}
\arguments{
\item{folder}{Directory to scan in folder mode; every file matching \code{pattern} is
converted and row-bound. Default \code{"."}. Ignored when \code{vcf_path} is supplied.}

\item{vcf_path}{Path to a single \code{.vep.vcf.gz} to convert (single-file mode).
\code{NULL} (default) selects folder mode.}

\item{pattern}{Regular expression identifying per-sample VCFs in folder mode.
Default \code{"_\\\\d+\\\\.vep\\\\.vcf\\\\.gz$"} (matches \verb{_01}, \verb{_02}, ...).}

\item{write_tsv}{Logical; if \code{TRUE}, also write the MAF as a TSV to \code{out_dir}.
Default \code{FALSE}.}

\item{write_rds}{Logical; if \code{TRUE}, also write the MAF as an \code{.rds} to \code{out_dir}.
Default \code{FALSE}.}

\item{write_xlsx}{Logical; if \code{TRUE}, also write the MAF as an \code{.xlsx} workbook
(single \code{"MAF"} sheet) to \code{out_dir}. Requires the \pkg{openxlsx} package (a
\code{Suggests} dependency); if it is not installed the export is skipped with a
warning. Default \code{FALSE}. Note: germline MAFs can be very large; Excel handles
them but the file is big and slow to open, so \code{write_rds}/\code{write_tsv} are better
for large tables. Excel also caps a sheet at 1,048,576 rows.}

\item{out_dir}{Output directory for written TSV/RDS/XLSX. \code{NULL} (default) uses the
input location/working directory. Only used when \code{write_tsv}/\code{write_rds}/\code{write_xlsx} is \code{TRUE}.}

\item{out_prefix}{Filename prefix for written outputs. \code{NULL} (default) derives one
from the input.}

\item{chunk_size}{Integer; number of VCF records processed per chunk (controls peak
memory and progress granularity). Default \code{25000L}.}

\item{ncbi_build}{Reference build label recorded in the MAF (\code{NCBI_Build}). Default
\code{"GRCh38"}.}

\item{add_genotype}{Logical; if \code{TRUE} (default) add the \code{Genotype} column.}

\item{strip_hgvs_prefix}{Logical; if \code{TRUE} (default) strip the Ensembl feature
prefix from \code{HGVSc}/\code{HGVSp}.}

\item{dedup_columns}{Logical; if \code{TRUE} (default) drop duplicate-named columns when
byte-for-byte identical (otherwise keep and warn).}

\item{drop_empty_cols}{Logical; if \code{TRUE}, drop columns that are entirely \code{NA}/blank.
Default \code{FALSE}.}

\item{add_abraom}{Logical; if \code{TRUE} (default) join the ABraOM SABE-609 allele
frequency as \code{ABraOM_AF}.}

\item{abraom_path}{Path to a local ABraOM annotation file. \code{NULL} (default) uses an
auto-managed cache, downloading from \code{abraom_url} if needed.}

\item{abraom_url}{URL of the ABraOM SABE-609 annotated release used when
\code{abraom_path} is \code{NULL}.}

\item{min_DP}{Numeric; keep only records with \code{DP > min_DP}. \code{NULL} (or \code{NA})
disables the depth filter. Default \code{10}.}

\item{min_GQ}{Numeric; keep only records with \code{GQ > min_GQ}. \code{NULL} (or \code{NA})
disables the genotype-quality filter. Default \code{30}.}

\item{genes}{Character vector of \code{Hugo_Symbol}s to keep (exact, case-insensitive),
or \code{NULL} (default) to keep all genes.}

\item{ncores}{Integer; number of worker processes for converting MULTIPLE input
files in parallel via \code{\link[parallel:mcdummies]{parallel::mclapply()}} (fork-based; Unix/macOS only).
Default \code{1L} runs sequentially and is byte-identical to previous behaviour.
Values \verb{> 1} only help when more than one VCF is being read (each file is an
independent task) and are clamped to \code{min(ncores, detectCores(), n_files)}. On
non-fork platforms it falls back to sequential. A single file is unaffected.}

\item{verbose}{Logical; if \code{TRUE} (default) print per-file and per-chunk progress
(file i/N, cumulative records, elapsed seconds).}
}
\value{
A \code{data.table} MAF: one row per variant allele, with MAF core columns, all
VEP CSQ fields, key GATK QC fields, \code{Tumor_Sample_Barcode}, and (when enabled) the
\code{Genotype} and \code{ABraOM_AF} columns. TSV/RDS files are written as a side
effect when \code{write_tsv}/\code{write_rds} is \code{TRUE}.
}
\description{
Converts VEP-annotated, single-sample germline VCFs (GATK HaplotypeCaller ->
CNN tranches -> Ensembl VEP, hg38) into a maftools-style table and returns it as
an in-memory \code{data.table} for downstream filtering (\code{\link[=gvr_filter]{gvr_filter()}}) and
summarisation (\code{\link[=gvr_summary]{gvr_summary()}}). In folder mode it finds every per-sample VCF,
converts each, and row-binds them into one combined MAF. The conversion uses base R
\itemize{
\item \pkg{data.table} only - it does NOT depend on \pkg{maftools}.
}
}
\details{
Output and behaviour:
\itemize{
\item Returns the final MAF \code{data.table}, one row per variant ALLELE
(multi-allelic sites are split).
\item A single most-severe transcript is chosen per allele (VEP severity ->
\code{CANONICAL} -> \code{MANE_SELECT} -> transcript id).
\item Columns include the MAF core fields, ALL VEP CSQ fields (read from the VCF
header), and key GATK QC fields. \code{FILTER} is retained as a column and ALL
variants (PASS and non-PASS) are kept.
\item \code{Tumor_Seq_Allele1}/\code{Tumor_Seq_Allele2} are zygosity-aware (vcf2maf-style),
and an optional \code{Genotype} column (\code{Tumor_Seq_Allele1/Tumor_Seq_Allele2}, e.g.
\code{"T/C"}) is added next to the alleles.
\item Each variant keeps its source sample in \code{Tumor_Sample_Barcode}.
\item Absent values are written as the empty string \code{""} (not \code{NA}); downstream
\code{\link[=gvr_filter]{gvr_filter()}} / \code{\link[=gvr_summary]{gvr_summary()}} treat \code{NA} and \code{""} identically as "missing".
}

Processing options:
\itemize{
\item MULTI-FILE: in folder mode, every file matching \code{pattern} (default
\code{"*_NN.vep.vcf.gz"}) is converted and row-bound.
\item HGVS CLEANUP (\code{strip_hgvs_prefix}): strips the Ensembl feature prefix from
\code{HGVSc}/\code{HGVSp} (e.g. \code{"ENST00000831140.1:n.1889G>A"} -> \code{"n.1889G>A"}).
\item DEDUP (\code{dedup_columns}): removes duplicate-named columns ONLY when their
values are byte-for-byte identical across all rows (otherwise keeps + warns).
\item ABraOM (\code{add_abraom}): joins the Brazilian ABraOM SABE-609 allele frequency
as the \code{ABraOM_AF} column (downloaded/cached from \code{abraom_url}).
\item GENOTYPE-QUALITY FILTER (\code{min_DP}/\code{min_GQ}): keeps a record iff
\code{DP > min_DP} AND \code{GQ > min_GQ}; mirrors
\verb{bcftools view -e 'FORMAT/DP<=X | FORMAT/GQ<=Y'}. Set either to \code{NULL} to
disable that field; set both to \code{NULL} to disable the genotype filter entirely.
\item GENE SUBSET (\code{genes}): restrict to a set of \code{Hugo_Symbol}s (exact,
case-insensitive).
}
}
\examples{
\dontrun{
## Folder mode: merge ALL *_NN.vep.vcf.gz into one MAF
maf <- read.gvr("/path/to/folder")

## Also write TSV + RDS outputs
maf <- read.gvr("/path/to/folder", write_tsv = TRUE, write_rds = TRUE,
                out_dir = "/path/to/results")

## Single-file mode
maf <- read.gvr(vcf_path = "/path/to/SAMPLE_01.vep.vcf.gz")

## DP/GQ genotype filter (ON by default; mirrors
##   bcftools view -e 'FORMAT/DP<=10 | FORMAT/GQ<=30')
maf <- read.gvr("/path/to/folder")                               # DP>10 & GQ>30
maf <- read.gvr("/path/to/folder", min_DP = NULL, min_GQ = NULL) # no DP/GQ filter
maf <- read.gvr("/path/to/folder", min_DP = 20,  min_GQ = 50)    # stricter

## Restrict to genes of interest (e.g. an MEN1/parathyroid panel)
maf <- read.gvr("/path/to/folder",
                genes = c("MEN1", "RET", "CDKN1B", "CDC73", "CASR", "AIP"))

## Then filter freely, e.g.:
maf[FILTER == "PASS" & Variant_Classification == "Missense_Mutation"]
}

}
\seealso{
\code{\link[=gvr_filter]{gvr_filter()}} to filter the returned MAF, \code{\link[=gvr_summary]{gvr_summary()}} to summarise it.

Other germlinevaR: 
\code{\link{gvr_filter}()},
\code{\link{gvr_oncoplot}()},
\code{\link{gvr_summary}()}
}
\author{
germlinevaR authors
}
\concept{germlinevaR}
