% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gvr_sum_plots.R
\name{gvr_sum_plots}
\alias{gvr_sum_plots}
\title{Export gvr_summary() plots as standalone image files}
\usage{
gvr_sum_plots(
  gvr,
  out_dir = ".",
  folder_name = "gvr_sum_plots",
  format = "png",
  width = 7,
  height = 5,
  dpi = 300,
  sample_col = "Tumor_Sample_Barcode",
  top_n_genes = 20,
  top_n_variants = 20,
  per_sample = TRUE,
  panel = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{gvr}{data.table or data.frame produced by \code{\link[=read.gvr]{read.gvr()}} /
\code{\link[=read.gvr.snpeff]{read.gvr.snpeff()}} / \code{\link[=read.gvr.dual]{read.gvr.dual()}}. Required columns:
\code{Hugo_Symbol}, \code{Variant_Classification}, \code{Variant_Type}, \code{IMPACT},
\code{CLIN_SIG}; \code{dbSNP_RS} is optional.}

\item{out_dir}{parent directory under which the output folder is created.
Default \code{"."} (current working directory).}

\item{folder_name}{name of the new folder created under \code{out_dir}.
Default \code{"gvr_sum_plots"}.}

\item{format}{one of \code{"png"} (default), \code{"pdf"}, \code{"svg"}, \code{"jpeg"},
\code{"tiff"}, \code{"bmp"}, \code{"eps"}, \code{"ps"}, \code{"tex"}, \code{"wmf"}. Mirrors the device
list of \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{width}{plot width in inches (passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}).
Default 7.}

\item{height}{plot height in inches. Default 5.}

\item{dpi}{resolution for raster formats (ignored for vector). Default 300.}

\item{sample_col}{column holding the sample identifier. Default
\code{"Tumor_Sample_Barcode"}.}

\item{top_n_genes}{number of top genes to keep in \code{top_genes} and per-sample
plots. Default 20.}

\item{top_n_variants}{number of top variants to keep in \code{top_variants}.
Default 20.}

\item{per_sample}{logical; if \code{FALSE}, skip per-sample plots and the
per-sample panel. Default \code{TRUE}.}

\item{panel}{logical; if \code{FALSE}, skip both panel images. Default \code{TRUE}.}

\item{verbose}{logical; print progress messages. Default \code{TRUE}.}
}
\value{
The output folder path, invisibly.
}
\description{
Produces the same cohort-level and per-sample plots that \code{\link[=gvr_summary]{gvr_summary()}}
renders in its HTML/PDF dashboard, but as individual image files and as
multi-panel composites, written into a new folder.
}
\details{
The function recomputes the summary sections from the table (it does not
depend on a prior call to \code{\link[=gvr_summary]{gvr_summary()}}), then writes:

Cohort-level plots (always produced):
\itemize{
\item \verb{top_genes.<ext>}: top genes by variant count
\item \verb{variant_classification.<ext>}: top variant classifications
\item \verb{impact.<ext>}: VEP IMPACT severity
\item \verb{top_variants.<ext>}: top variants by \code{dbSNP_RS} recurrence
(omitted if the column is missing or all empty)
}

Per-sample plots (when \code{per_sample = TRUE}, one file per sample):
\itemize{
\item \verb{per_sample/top_genes__<sample>.<ext>}
}

Panels (when \code{panel = TRUE}):
\itemize{
\item \verb{panel_cohort.<ext>}: 2x2 grid of the four cohort plots
\item \verb{panel_per_sample.<ext>}: grid of every per-sample plot
(omitted if \code{n_samples == 1} since it would duplicate the
standalone per-sample image)
}

All files are written under \verb{out_dir/folder_name/}. Existing files of the
same name are overwritten; other files in the folder are left alone.
}
\examples{
## Load the shipped example table and write plots to a temp directory
gvr <- readRDS(system.file("extdata", "example_gvr.rds",
    package = "germlinevaR"))
out_dir <- gvr_sum_plots(gvr, out_dir = tempdir(), verbose = FALSE)
dir.exists(out_dir)

## Write PDF outputs into a named subfolder under tempdir()
gvr_sum_plots(gvr, out_dir = tempdir(), folder_name = "S6_plots",
    format = "pdf", verbose = FALSE)
}
\seealso{
\code{\link[=gvr_summary]{gvr_summary()}} for the dashboard view, \code{\link[=read.gvr]{read.gvr()}} for the
reader.
}
\author{
Thiago Loreto Matos
}
\concept{germlinevaR plotting}
