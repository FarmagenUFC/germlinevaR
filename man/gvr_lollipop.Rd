% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gvr_lollipop.R
\name{gvr_lollipop}
\alias{gvr_lollipop}
\title{Per-gene amino-acid lollipop plot for a germline gvr table}
\usage{
gvr_lollipop(
  gvr,
  gene,
  vc_keep = NULL,
  protein_length = NULL,
  protein_length_strict = FALSE,
  domains = "auto",
  organism = 9606L,
  cache_dir = NULL,
  label_top = 5L,
  domain_label_min_frac = 0.05,
  domain_label_mode = c("name", "id", "number", "none", "auto"),
  domain_name_abbrev = TRUE,
  domain_label_position = c("inside", "below"),
  domain_label_wrap = TRUE,
  hotspot_window = 20L,
  hotspot_min_n = 4,
  stem_alpha = 0.6,
  point_size = 3,
  stem_color = "grey50",
  bar_color = "grey85",
  bar_border = "grey40",
  base_size = 12,
  axis_text_size = 11,
  axis_title_size = 12,
  axis_text_color = "grey20",
  axis_title_color = "black",
  axis_line_color = "grey40",
  axis_line_width = 0.4,
  out_dir = ".",
  out_subdir = "gvr_lollipop",
  out_prefix = gene,
  width = 10,
  height = 4,
  dpi = 300,
  variant_palette = "gvr",
  domain_palette = "okabe_ito",
  verbose = TRUE
)
}
\arguments{
\item{gvr}{An MAF-like \code{data.table} / \code{data.frame} from \code{\link[=read.gvr]{read.gvr()}}, or any
compatible table with at least \code{Hugo_Symbol}, \code{HGVSp_Short},
\code{Variant_Classification}, \code{Tumor_Sample_Barcode}, \code{Protein_position}.}

\item{gene}{Character(1). The gene symbol (matched against \code{Hugo_Symbol}).}

\item{vc_keep}{Character vector of \code{Variant_Classification} values to keep.
\code{NULL} (default) uses the 9-class protein-altering set from
\code{\link[=gvr_filter]{gvr_filter()}}.}

\item{protein_length}{Integer(1) or \code{NULL}. Sets the x-axis length (in
amino acids). Resolved with the following precedence:
\enumerate{
\item UniProt canonical sequence length, fetched via the same
UniProt REST query used for \code{domains = "auto"}. Wins by default;
see \code{protein_length_strict} to opt out.
\item User-supplied value when \code{protein_length_strict = TRUE}, or
when UniProt is unreachable.
\item Mode of the \verb{<pos>/<total>} totals parsed from the table
\code{Protein_position} column (entries without a \code{/} are skipped).
\item \code{ceiling(max(positions) * 1.1)} fallback.
}
The final value is always at least \code{max(positions)} so no mutation is
clipped. A one-line \code{message()} is emitted when UniProt overrides a
user-supplied value by more than 5\%.}

\item{protein_length_strict}{Logical(1). When \code{TRUE}, the user's
\code{protein_length} is kept verbatim and the UniProt canonical length is
not used (the max-position safety net still applies). Default \code{FALSE}.}

\item{domains}{\code{"auto"}, \code{NULL}, or a \code{data.frame}. Default \code{"auto"}:
domains are fetched from the EBI InterPro REST API and cached. \code{NULL}
draws a plain bar with no domain rectangles. A \code{data.frame} with columns
\code{start}, \code{end} (and optional \code{name}, \code{color}) draws custom domain
rectangles. See \strong{Details} for the three accepted forms.}

\item{organism}{Integer or string. NCBI taxonomy id used when
\code{domains = "auto"} (passed through to the UniProt search as
\code{organism_id}). Default \code{9606L} (human). Ignored when \code{domains} is not
\code{"auto"}.}

\item{cache_dir}{\code{NULL}, a directory path, or \code{FALSE}. Controls the
on-disk cache used for \code{domains = "auto"}. \code{NULL} (default) triggers the
precedence chain documented in \strong{Details}. \code{FALSE} disables on-disk
caching. Ignored when \code{domains} is not \code{"auto"}.}

\item{label_top}{Integer(1). Number of top-counted positions to label.
\code{0} disables labels, \code{Inf} labels every position. Default \code{5L}.}

\item{domain_label_min_frac}{Numeric(1) in \verb{[0, 1]}. Minimum domain width
as a fraction of \code{protein_length} for a domain label to be rendered in
\code{"name"} and \code{"id"} modes. Default \code{0.05} (5\%). Lower this for long
proteins (e.g. \code{0.01} so that even small domains get labelled). Ignored
when \code{domain_label_mode = "number"} (all domains labelled) or \code{"none"}.}

\item{domain_label_mode}{One of \code{"name"} (default), \code{"id"}, \code{"number"},
or \code{"none"}. Controls how domain rectangles are labelled.
\itemize{
\item \code{"name"}: human-readable InterPro name, repelled below the bar with
leader lines (uses \code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}} when available).
\item \code{"id"}: InterPro accession (e.g. \code{IPR011615}); falls back to name
when accession is missing (user-supplied data.frame without it).
\item \code{"number"}: numbers 1..N centered in each rectangle, with a
companion \code{"Domains"} legend mapping number to full name. Requires
the optional \code{ggnewscale} package; without it, only numbers render.
\item \code{"auto"}: opt-in heuristic. Use \code{"number"} when
\code{protein_length > 2000} aa AND \verb{>= 5} domains are drawn
(long, densely-annotated proteins where in-plot names overlap),
otherwise use \code{"name"}. With \code{verbose = TRUE}, the resolved mode
and reason are printed.
\item \code{"none"}: no labels (rely on the variant legend only).
}}

\item{domain_name_abbrev}{Logical(1). When \code{TRUE} (default), apply a small
set of substitutions to compress verbose InterPro domain names (e.g.
"von Willebrand factor, type D domain" becomes "VWF type-D") so that
labels fit inside narrower rectangles in \code{domain_label_mode = "name"}.
Set \code{FALSE} to keep the raw InterPro names verbatim.}

\item{domain_label_position}{One of \code{"inside"} (default) or \code{"below"}.
Controls where InterPro domain labels render relative to the domain
rectangle. \code{"inside"} places labels centred inside each rectangle with
an automatically chosen black/white text colour for contrast against the
domain fill (WCAG luminance rule). \code{"below"} reproduces the legacy
layout used in earlier versions (labels under the bar, leader lines from
each rectangle).

When \code{domain_label_position = "inside"}, labels that don't fit inside
their domain rectangle (estimated by character count vs rectangle width)
fall back to the \code{"below"} style with a leader line on a per-domain
basis so no information is lost. A verbose message reports the count of
overflowing labels.}

\item{domain_label_wrap}{Logical(1). When \code{TRUE} (default), inside-mode
domain labels that don't fit on one line are wrapped onto a second
line at the nearest comma (or at a space if no commas) and the font is
shrunk in 10\% steps down to a floor of 60\% of base. Wrapping is
per-domain: labels that already fit on one line stay unchanged. If a
label still overflows after the 2-line + 60\% font cascade, it falls
back to the existing below-bar leader (so no information is lost).
Set \code{FALSE} to keep the pre-Phase-L behaviour: 1-line labels only, with
any overflow going directly to the below-bar fallback.}

\item{hotspot_window}{Integer(1). Sliding-window width (in amino
acids) used to detect mutation hotspots. A hotspot is a region
containing at least \code{hotspot_min_n} distinct variant positions
within \code{hotspot_window} aa. Drawn as a soft translucent vertical
band behind the bar/domains/stems. Default \code{20L} (publication-tight
rule). Increase for noisier/exploratory cohorts.}

\item{hotspot_min_n}{Numeric(1). Minimum distinct variant positions
inside a \code{hotspot_window}-wide region for it to be drawn as a
hotspot band. Default \code{4}. Pass \code{Inf} to disable hotspot detection
entirely. Counting uses unique aa positions (not sample counts), so
a single recurrently-hit position does not by itself create a band.}

\item{stem_alpha}{Numeric(1) in \verb{[0, 1]}. Opacity of lollipop stems.
Default \code{0.6}. Lower this further for very dense plots; raise to \code{1}
for the original solid stems.}

\item{point_size}{Numeric(1). Dot size. Default \code{3}.}

\item{stem_color}{Character(1). Stem (vertical line) colour. Default
\code{"grey50"}.}

\item{bar_color}{Character(1). Protein-body bar fill colour. Default
\code{"grey85"}.}

\item{bar_border}{Character(1) or \code{NA}. Protein-body bar border colour.
Default \code{"grey40"}.}

\item{base_size}{Numeric(1). ggplot2 base font size. Default \code{12}.}

\item{axis_text_size}{Numeric(1). Font size of axis tick labels (aa
positions on x, count on y). Default \code{11}, matching the existing visual
output. Set to \code{12} or higher for publication-style figures.}

\item{axis_title_size}{Numeric(1). Font size of axis titles ("Amino acid
position" / "Variant count"). Default \code{12}. Set to \code{14} for sibling-
package publication defaults.}

\item{axis_text_color}{Character(1). Colour of axis tick labels. Default
\code{"grey20"} (current visual). Set to \code{"black"} for sibling-package
publication defaults.}

\item{axis_title_color}{Character(1). Colour of axis titles. Default
\code{"black"}.}

\item{axis_line_color}{Character(1). Colour of axis lines and ticks.
Default \code{"grey40"} (current visual). Set to \code{"black"} for sibling-
package publication defaults.}

\item{axis_line_width}{Numeric(1). Line width (in \code{ggplot2} linewidth
units) of axis lines and ticks. Default \code{0.4} (current visual). Set to
\code{0.8} for sibling-package publication defaults.}

\item{out_dir}{Character(1) or \code{NULL}. Output directory root for the
SVG/PNG files. Created if it does not exist. Default \code{"."} (current
working directory), matching \code{\link[=gvr_plot]{gvr_plot()}}. Set to \code{NULL} to suppress
file output. By default a \verb{gvr_lollipop/} subfolder is created inside
\code{out_dir} (see \code{out_subdir}).}

\item{out_subdir}{Character(1) or \code{NULL}. Subfolder under \code{out_dir} to
collect lollipop outputs. Default \code{"gvr_lollipop"}, matching the
\code{\link[=gvr_genepos.plot]{gvr_genepos.plot()}} / \code{\link[=gvr_summary]{gvr_summary()}} convention. Set to \code{NULL} (or
\code{""}) to write files directly into \code{out_dir} with no subfolder.}

\item{out_prefix}{Character(1) or \code{NULL}. Filename prefix; output files are
\verb{<out_prefix>_lollipop.svg} / \verb{<out_prefix>_lollipop.png}. Default \code{gene}
(the gene symbol), so with default \code{out_dir = "."} and
\code{out_subdir = "gvr_lollipop"}, \code{gvr_lollipop(f, "TP53")} writes
\code{./gvr_lollipop/TP53_lollipop.svg} and \code{./gvr_lollipop/TP53_lollipop.png}.
Set to \code{NULL} to suppress file output.}

\item{width}{Numeric(1). Plot width in inches. Default \code{10}.}

\item{height}{Numeric(1). Plot height in inches. Default \code{4}.}

\item{dpi}{Numeric(1). PNG resolution. Default \code{300}.}

\item{variant_palette}{Character. Variant colour assignment. Accepts three
forms. (1) A palette name (e.g. \code{"gvr"}, \code{"nature"}, \code{"set2"}); the
default \code{"gvr"} is the semantic palette where, e.g., \code{Missense_Mutation}
is always green and \code{Nonsense_Mutation} always orange regardless of which
classes are present. Any other palette name is treated as positional
ordinal (colours assigned in the order classes appear in the data).
(2) A named character vector of explicit hex codes for specific
\code{Variant_Classification} values, e.g.
\code{c(Missense_Mutation = "#FF0000", Nonsense_Mutation = "#000000")};
classes not listed inherit the \code{"gvr"} defaults. (3) A mixed vector
with one unnamed element used as the fallback palette name, e.g.
\code{c(Missense_Mutation = "#FF00FF", "nature")} (Missense magenta, all
other classes from the \code{nature} palette). Use \code{\link[=gvr_list_palettes]{gvr_list_palettes()}} to
discover names and \code{\link[=gvr_color_palette]{gvr_color_palette()}} to inspect colours.}

\item{domain_palette}{Character. Palette used for InterPro domain
rectangle fills. Accepts the same three forms as \code{variant_palette}:
palette name (default \code{"okabe_ito"}), named vector keyed by accession
or domain name, or mixed vector with fallback palette name. Unlike
\code{variant_palette} there is no \code{"gvr"} semantic special case; the
default cycles a 9-colour colour-blind-safe palette across all domains.}

\item{verbose}{Logical(1). If \code{TRUE} (default), emit progress messages
(counts, dropped rows, file paths, cache hits, network calls).}
}
\value{
A ggplot2 object. By default SVG and PNG files are also written to
disk as a side effect. Set \code{out_dir = NULL} to suppress file output.
}
\description{
Builds a ggplot2 lollipop plot of every protein-altering variant of a single
gene in a \code{read.gvr()} / \code{gvr_filter()} / \code{gvr_novel()} table. Each amino-acid
position is a stem; each sample-variant carried at that position is one dot
stacked on the stem; dots are coloured by \code{Variant_Classification} using the
package's colourblind-safe palette. A horizontal protein-body bar is drawn
along the x-axis (lollipop-style); protein domains are
overlaid as coloured rectangles on top of that bar. By default, domains are
fetched automatically from the EBI InterPro REST API (\code{domains = "auto"}).
Pass \code{domains = NULL} for a plain bar with no domains, or supply a
\code{data.frame} for custom domain coordinates. By default writes both an
\code{.svg} and a \code{.png} to disk (matching \code{\link[=gvr_plot]{gvr_plot()}} behaviour).
}
\details{
Variant classes are filtered down to the canonical protein-altering set (the
same 9 classes used by \code{\link[=gvr_filter]{gvr_filter()}} when \code{vc_nonSyn = TRUE}):
\code{Frame_Shift_Del}, \code{Frame_Shift_Ins}, \code{Splice_Site}, \code{Translation_Start_Site},
\code{Nonsense_Mutation}, \code{Nonstop_Mutation}, \code{In_Frame_Del}, \code{In_Frame_Ins},
\code{Missense_Mutation}. The default can be overridden with the \code{vc_keep}
argument.

Amino-acid positions are extracted from \code{HGVSp_Short} using the regex
\verb{^p\\\\.[A-Z*]([0-9]+)}. Rows whose \code{HGVSp_Short} does not match (complex
indels with \verb{p.M1?}, splice variants without \code{p.}, etc.) are dropped with a
warning showing how many rows were dropped. Multi-amino-acid descriptors
(e.g. \code{p.D1850_E1852del}) match the first amino-acid number, which is the
standard 5'-most position convention.

Protein length (the x-axis upper bound) is derived from the
\code{Protein_position} column: each value is a string \code{"<current>/<total>"}. The
most-common \code{total} across the gene's rows is used. If no row has a
parseable \code{total}, the x-axis falls back to \code{max(position) * 1.1}. The
\code{protein_length} argument overrides this.

Stacking: each surviving sample-variant becomes one dot. Dots sharing the
same amino-acid position are stacked vertically, so the height of a stack
equals the count of sample-variants at that position. Stems run from the
top of the protein-body bar up to the top of the stack.

Labels: the top-\code{label_top} positions by count are annotated with their
\code{HGVSp_Short} (e.g. \code{p.R175H}). When multiple distinct \code{HGVSp_Short} share
a position, the most-common one is shown followed by \verb{(+N more)}. Set
\code{label_top = 0} to disable labels, \code{label_top = Inf} to label every
position. If \pkg{ggrepel} is installed, labels are placed via
\code{ggrepel::geom_text_repel()}; otherwise a plain \code{geom_text()} is used.

Protein-body bar: a thin horizontal rectangle is drawn straddling \code{y = 0}
from x = 0 to x = \code{protein_length}. Bar height scales with the visible
stack height (about 4 percent of the y range, clamped to \verb{[0.15, 0.5]}).
The y-axis is expanded slightly below zero to accommodate the bar.

Domains: \code{domains} accepts three forms:
\itemize{
\item \code{"auto"} (default) - the function calls the EBI InterPro REST API
for the \code{gene} (gene -> UniProt accession via \code{rest.uniprot.org},
then UniProt -> InterPro domains via \code{www.ebi.ac.uk/interpro}),
keeps only InterPro-integrated entries (those with
\code{source_database == "interpro"}, e.g. \code{IPR011615}), caches the
result, and converts to the same \code{data.frame} shape as below.
Requires the optional packages \pkg{httr} and \pkg{jsonlite}; if
either is missing, or any HTTP / parsing step fails, a warning is
emitted and the plot still renders with the plain bar.
\item \code{NULL} - no domain rectangles, just the plain bar.
\item A \code{data.frame} with required columns \code{start}, \code{end} and optional
columns \code{name} (label, shown when the rectangle is wide enough) and
\code{color} (hex code or R colour name; auto-assigned from a
colourblind-safe palette when missing). Rows entirely outside
\verb{[0, protein_length]} are dropped; partial overlaps are clipped.
}

Cache (used when \code{domains = "auto"}, the default): the resolved cache
directory is picked from the first writable entry in this precedence chain:
\enumerate{
\item \code{cache_dir} argument (explicit override; pass \code{FALSE} to disable
on-disk caching entirely).
\item Environment variable \code{GVR_CACHE_DIR} (useful for HPC \verb{/scratch/$USER}).
\item R option \code{getOption("germlinevaR.cache_dir")} (for \code{.Rprofile}).
\item \code{tools::R_user_dir("germlinevaR", "cache")} (XDG-compliant default).
\item \code{file.path(tempdir(), "germlinevaR_cache")} (last-resort, per-session).
}
Cached files: \verb{<cache_dir>/domains_interpro_<GENE>_<ORG>.rds}. To force a
refresh, delete the file (or use the \code{\link[=gvr_domain_cache_clear]{gvr_domain_cache_clear()}} helper).

Empty-gene behaviour: if no row matches the gene, or none survives the
\code{vc_keep} filter, or none has a parseable amino-acid position, the function
issues a warning and returns a ggplot2 object with a single centered
"No protein-altering variants for \if{html}{\out{<GENE>}}" annotation. No files are written
for empty-gene plots.

File output: by default the function writes both SVG and PNG into a
\verb{gvr_lollipop/} subfolder under \code{out_dir} (default \code{out_dir = "."}, so files
land in \verb{./gvr_lollipop/}), using \code{out_prefix} (default \code{gene}) as the
filename prefix:
\itemize{
\item \verb{<out_dir>/<out_subdir>/<out_prefix>_lollipop.svg} - vector (lossless)
\item \verb{<out_dir>/<out_subdir>/<out_prefix>_lollipop.png} - raster at \code{dpi}
}
The subfolder name is controlled by \code{out_subdir} (default \code{"gvr_lollipop"},
matching the \code{\link[=gvr_genepos.plot]{gvr_genepos.plot()}} / \code{\link[=gvr_summary]{gvr_summary()}} convention); set
\code{out_subdir = NULL} (or \code{""}) to write directly into \code{out_dir} with no
subfolder. Both files are first rendered to \code{tempdir()} then \code{cp}'d to the
final directory, so S3-backed FUSE mounts (e.g. \verb{/mnt/results/}) work
without 0-byte issues. Set \code{out_dir = NULL} to suppress file output and
return only the ggplot2 object. The function always returns the ggplot2
object regardless of whether files were written.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  ## Load the shipped example table; suppress file output and network call
  gvr <- readRDS(system.file("extdata", "example_gvr.rds",
                             package = "germlinevaR"))
  ## OR4F5 is the first gene in the example; use domains=NULL to skip REST
  p <- gvr_lollipop(gvr, gene = "OR4F5", domains = NULL,
                    out_dir = NULL, verbose = FALSE)
  class(p)
}

\dontrun{
  gvr <- read.gvr("vcf_dir/", pattern = "\\\\.vep\\\\.vcf\\\\.gz$")
  f   <- gvr_filter(gvr)

  ## default: auto-fetches InterPro domains, auto-saves to "."
  p <- gvr_lollipop(f, "TP53")

  ## second call uses cache, no network
  gvr_lollipop(f, "TP53")

  ## plain bar, no domain rectangles
  gvr_lollipop(f, "TP53", domains = NULL)

  ## with user-supplied domains (TP53 example - canonical UniProt P04637)
  tp53_dom <- data.frame(
    start = c(95,   323),
    end   = c(288,  356),
    name  = c("DNA-binding", "Tetramerization"),
    color = c("#FF9400", "#75A025")
  )
  gvr_lollipop(f, "TP53", domains = tp53_dom)

  ## non-human cohort: pass the NCBI taxonomy id
  gvr_lollipop(f, "Trp53", organism = 10090) # mouse

  ## HPC: send the cache to scratch
  Sys.setenv(GVR_CACHE_DIR = "/scratch/$USER/germlinevaR_cache")
  gvr_lollipop(f, "MUC16")

  ## disable on-disk caching entirely (CI / shared scratch)
  gvr_lollipop(f, "BRCA1", cache_dir = FALSE)

  ## suppress file output, return ggplot2 only
  p <- gvr_lollipop(f, "BRCA1", out_dir = NULL)
  print(p)

  ## Long protein, two label strategies side-by-side
  ## MUC19 has 9-12 InterPro domains clustered in aa 470-1604, so
  ## name-mode labels overlap. Number-mode keeps the in-plot annotation
  ## compact and routes domain names into a side legend.
  gvr_lollipop(f, "MUC19", domain_label_mode = "name")
  gvr_lollipop(f, "MUC19", domain_label_mode = "number")

  ## Opt-in heuristic chooses between "name" and "number" automatically
  ## (uses "number" when protein > 2000 aa AND >= 5 InterPro domains).
  gvr_lollipop(f, "MUC19", domain_label_mode = "auto")

  ## Highlight hotspots: clusters of >= 4 variants within 20 aa
  ## (the default; set hotspot_min_n = Inf to disable).
  gvr_lollipop(f, "TP53", hotspot_window = 20, hotspot_min_n = 4)

  ## Inside-rectangle domain labels (new default in Phase J)
  gvr_lollipop(f, "TP53", domain_label_position = "inside")

  ## Legacy below-rectangle labels with leader lines
  gvr_lollipop(f, "TP53", domain_label_position = "below")

  ## Sibling-package publication defaults for axes
  gvr_lollipop(f, "TP53",
               axis_text_size = 12, axis_title_size = 14,
               axis_text_color = "black", axis_title_color = "black",
               axis_line_color = "black", axis_line_width = 0.8)

  ## Colour-blind-safe variant palette (positional, not semantic)
  gvr_lollipop(f, "TP53", variant_palette = "okabe_ito")

  ## Override Missense colour only; rest stay GVR semantic
  gvr_lollipop(f, "TP53",
               variant_palette = c(Missense_Mutation = "#FF00FF"))

  ## Override Missense + use Nature palette for the remaining classes
  gvr_lollipop(f, "TP53",
               variant_palette = c(Missense_Mutation = "#FF00FF", "nature"))

  ## Domain rectangles coloured along the viridis gradient
  gvr_lollipop(f, "MUC19", domain_palette = "viridis")

  ## List available palettes / inspect colours
  gvr_list_palettes()
  gvr_color_palette("nature", n = 5)
}

}
\seealso{
\code{\link[=read.gvr]{read.gvr()}}, \code{\link[=gvr_filter]{gvr_filter()}}, \code{\link[=gvr_novel]{gvr_novel()}}, \code{\link[=gvr_summary]{gvr_summary()}},
\code{\link[=gvr_plot]{gvr_plot()}}, \code{\link[=gvr_domain_cache_clear]{gvr_domain_cache_clear()}}
}
